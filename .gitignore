# Secrets
.env
.env.local
*.pem
*.key

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Large upstream data (not committed — reingest from source per db/README.md)
*.log
*.jsonl.tmp
db/data_*.jsonl
db/data_*.tsv
db/data_*.json
db/data_trials_*.csv
db/data_drug_master_lookup.csv
db/data_program_master.csv

# Model artifacts / training telemetry
catboost_info/
*.model
*.pkl
*.joblib

# Structural versioned re-pull: raw source downloads are streamed and never kept;
# ignore any raw/intermediate spillover. Only the lean derived data/versioned/*.csv
# (per-release per-gene summaries) are committed.
data/versioned/**/raw/
data/versioned/*.json.gz
data/versioned/*.parquet
data/versioned/*_raw.csv
*.snappy.parquet
*_association_data.json.gz
